# DUVELA EXAM audio

Place the ElevenLabs MP3 files in the `mt1` … `mt5` folders using the exact
filenames from `ELEVENLABS_HOEREN_SCRIPT.md`. Until an MP3 is present, the exam
runner automatically uses the browser's German voice as a fallback.
