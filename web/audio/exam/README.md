# DUVELA EXAM audio

Place the MP3 files in the `mt1` … `mt5` folders using the exact
filenames from `HOEREN_AUDIO_SCRIPT.md`. Until an MP3 is present, the exam
runner automatically uses the browser's German voice as a fallback.
