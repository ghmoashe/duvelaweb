(function(){
 function enhance(){const form=document.querySelector('#onboardingForm'),step=document.querySelector('#onboardingKicker');if(!form||!step||!step.textContent.includes('STEP 2')||document.querySelector('#ob-learner-profile-extra'))return;const box=document.createElement('div');box.id='ob-learner-profile-extra';box.className='wide';box.innerHTML='<div class="ob-extra-title">Personal details</div><div class="ob-extra-grid"><label>First name<input name="firstName" placeholder="Your first name"></label><label>Last name<input name="lastName" placeholder="Your last name"></label><label>Username<input name="username" placeholder="@username"></label><label>Date of birth<input name="dob" type="date"></label><label>Country<input name="country" placeholder="For example: Germany"></label><label>Gender<select name="gender"><option value="">Prefer not to say</option><option>Male</option><option>Female</option><option>Other</option></select></label></div>';form.appendChild(box);form.addEventListener('submit',()=>setTimeout(async()=>{const c=window.DuvelaWebConfig.createSupabaseClient(),u=(await c.auth.getUser()).data.user;if(!u)return;const d=new FormData(form);const first=String(d.get('firstName')||'').trim(),last=String(d.get('lastName')||'').trim();await c.from('profiles').update({full_name:[first,last].filter(Boolean).join(' ')||null,country:String(d.get('country')||'').trim()||null,updated_at:new Date().toISOString()}).eq('id',u.id);},120),{once:true});}
 new MutationObserver(enhance).observe(document.documentElement,{subtree:true,childList:true});
})();
